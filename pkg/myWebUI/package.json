{
	"name": "myWebUI",
	"type": "module",
	"version": "1.0.0",
	"scripts": {
		"build": "shx cp -r src build",
		"preview": "serve build",
		"ci": "run-s build",
		"clean": "rimraf build node_modules dist"
	},
	"devDependencies": {
		"npm-run-all2": "^8.0.4",
		"rimraf": "^6.0.1",
		"serve": "^14.2.4",
		"shx": "^0.4.0"
	}
}
