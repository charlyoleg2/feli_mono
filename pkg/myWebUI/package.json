{
	"name": "myWebUI",
	"type": "module",
	"version": "1.0.1",
	"scripts": {
		"build": "shx cp -r src build",
		"preview": "serve build",
		"ci": "run-s build",
		"clean": "rimraf build node_modules dist"
	},
	"devDependencies": {
		"npm-run-all2": "^8.0.4",
		"rimraf": "^6.1.2",
		"serve": "^14.2.5",
		"shx": "^0.4.0"
	}
}
